
--Initialize an empty table before everything else to allow addition of new characters
--simply by dropping in new scripts. Just add a call to table.insert(charaConfigs, ??)
--at the end of every character definition file.
charaConfigs = { }
